#!/usr/bin/env bash
# dont forget to add this name to etc/hosts on localhost, otherwise your "sudo" actions will get slow
# 127.0.0.1 localhost XPS
# ::1 localhost XPS
sudo hostnamectl set-hostname 'XPS'